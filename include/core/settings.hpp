#pragma once

inline constexpr int BLOCK = 64;

inline constexpr int RENDERING_WIDTH = 10;
inline constexpr int RENDERING_HEIGHT = 6;

inline constexpr int SCREEN_WIDTH = RENDERING_WIDTH * BLOCK;
inline constexpr int SCREEN_HEIGHT = RENDERING_HEIGHT * BLOCK;

inline constexpr int FONT_SIZE = 32;
